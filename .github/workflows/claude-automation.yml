name: Claude Enhancement

on:
  issues:
    types: [opened, edited]
  workflow_dispatch:

jobs:
  enhance:
    runs-on: ubuntu-latest
    # Remove this line: if: contains(github.event.issue.body, 'claude')
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Test Action
      run: echo "Claude automation is working!"
